{
  "name": "foc-observer-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "cp ../shared/system-context.ts src/system-context.ts && cp ../indexer/src/schema-defs.ts src/schema-defs.ts && cp ../shared/table-metadata.ts src/table-metadata.ts",
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/main.js",
    "test": "vitest run",
    "test:live": "LIVE_TEST=1 vitest run"
  },
  "dependencies": {
    "@hono/node-server": "^2.1.1",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "hono": "^4",
    "libpg-query": "^17.7.3",
    "pg": "^8.23.0",
    "viem": "^2.56.3",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/pg": "^8.23.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
